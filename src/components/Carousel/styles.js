import styled from "styled-components";

export const BoxTextCarousel = styled.div`
    font-size: ${props => props.fontSize};
    font-style: normal;
    color: ${props => props.color};
    line-height: normal;
`